[project]
name = "jsk-ai-token-saver"
version = "0.3.0"
description = "Active token-saving skill with safety-first verification for AI agents"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "tiktoken>=0.13.0,<1",
]

[tool.uv]
package = false
